<h1>Welcome to WIKIFAKEOUS</h1>

<% @articles.each do |article| %>
<h2><%= link_to article.title, article_path(article) %></h2>
<p><%= link_to "Delete this article", article_path(article), method: :delete, data: { confirm: "Are you sure you want to delete this article?" } %></p>
<% end %>

<p><%= link_to "Add an article", new_article_path %></p>
